{
  "name": "streaming-api",
  "version": "1.0.0",
  "dependencies": {},
  "devDependencies": {
    "jest": "^24.8.0",
    "serverless": "^1.41.1"
  },
  "scripts": {
    "test": "jest --coverage --verbose tests",
    "deploy": "serverless deploy --region eu-west-2",
    "undeploy": "serverless remove --region eu-west-2",
    "logs": "serverless logs --region eu-west-2 -f start"
  }
}
